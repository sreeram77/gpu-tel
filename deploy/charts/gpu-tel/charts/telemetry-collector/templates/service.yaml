apiVersion: v1
kind: Service
metadata:
  name: {{ include "telemetry-collector.fullname" . }}
  labels:
    {{- include "telemetry-collector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "telemetry-collector.selectorLabels" . | nindent 4 }}
