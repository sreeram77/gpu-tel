apiVersion: v1
kind: Service
metadata:
  name: {{ include "telemetry-streamer.fullname" . }}
  labels:
    {{- include "telemetry-streamer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "telemetry-streamer.selectorLabels" . | nindent 4 }}
